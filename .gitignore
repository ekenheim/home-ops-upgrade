# Trash
.DS_Store
Thumbs.db
# k8s
kubeconfig
.decrypted~*.yaml
.config.env
*.agekey
*.pub
*.key
# etcd snapshots hold every cluster secret in plaintext. Never commit one.
etcd-backup.db
*.etcd-snapshot

# Local docs (not committed)
docs/
# Ansible
.venv*
# Taskfile
.tasks
.cursor
