---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./helmrelease.yaml
  # Placeholders so run pods start; ExternalSecrets overwrite when sync succeeds
  - ./bandit-secrets-placeholder.yaml
