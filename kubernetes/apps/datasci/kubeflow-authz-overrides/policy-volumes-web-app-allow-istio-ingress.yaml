---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: volumes-web-app-allow-istio-ingress
  namespace: kubeflow
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: volumes-web-app
  # Allow requests to reach the volumes web app; auth is enforced at the deployKF gateway (oauth2-proxy ext_authz).
  rules:
    - {}


