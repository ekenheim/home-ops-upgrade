# Patch to use existing istio-ingress instead of default ingressgateway
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: kubeflow-gateway
  namespace: kubeflow
spec:
  selector:
    # Change from default 'istio: ingressgateway' to match existing ingress
    istio: ingress

