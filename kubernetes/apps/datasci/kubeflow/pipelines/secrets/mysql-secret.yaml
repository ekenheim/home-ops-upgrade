---
# MySQL secret for cache-server (uses bundled MySQL with empty password)
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: kubeflow
type: Opaque
stringData:
  username: "root"
  password: ""  # MySQL allows empty password (MYSQL_ALLOW_EMPTY_PASSWORD=true)
