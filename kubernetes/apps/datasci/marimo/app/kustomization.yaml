---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - externalsecret.yaml
  - secret.sops.yaml
  - helmrelease.yaml
  - ../../../../templates/gatus/guarded

configMapGenerator:
  - name: marimo-seed-notebooks
    files:
      - bayesian_bandits.py=../notebooks/bayesian_bandits.py
      - marimo.toml=../config/marimo.toml
    options:
      disableNameSuffixHash: true
