---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qdrant
  namespace: datasci
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block
