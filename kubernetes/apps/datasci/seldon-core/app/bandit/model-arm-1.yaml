---
# Seldon v2 Model — bandit arm-1.
# Mirror of arm-0; artifact written to s3://bandit/models/arm-1/latest by Dagster.
apiVersion: mlops.seldon.io/v1alpha1
kind: Model
metadata:
  name: bandit-arm-1

  labels:
    app.kubernetes.io/name: bandit
    app.kubernetes.io/component: seldon-arm
spec:
  storageUri: "s3://bandit/models/arm-1/latest"
  secretName: seldon-core-secret
  requirements:
    - mlflow
  memory: 100Mi
