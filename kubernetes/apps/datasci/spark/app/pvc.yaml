---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark
  namespace: datasci
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-block
  resources:
    requests:
      storage: 10Gi
