---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memory-mcp
  namespace: datasci
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 1Gi
