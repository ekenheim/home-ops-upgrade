---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-nfs
  namespace: default
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: used-nfs
  resources:
    requests:
      storage: 1Mi # Size doesn't matter for NFS
