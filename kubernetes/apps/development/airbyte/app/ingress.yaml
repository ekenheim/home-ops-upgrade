---
# Standalone Ingress for Airbyte (chart ingress not created by Airbyte Helm in this setup)
# Run: kubectl get svc -n development | grep airbyte  to confirm service names
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: airbyte
  namespace: development
  labels:
    app.kubernetes.io/name: airbyte
  annotations:
    external-dns.alpha.kubernetes.io/target: internal.${SECRET_DOMAIN}
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: Airbyte
    gethomepage.dev/group: Development
    gethomepage.dev/icon: airbyte.png
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
spec:
  ingressClassName: internal
  rules:
    - host: airbyte.${SECRET_DOMAIN}
      http:
        paths:
          # Connector builder first (more specific)
          - path: /api/v1/connector_builder
            pathType: Prefix
            backend:
              service:
                name: airbyte-airbyte-connector-builder-server-svc
                port:
                  number: 80
          - path: /connector-builder
            pathType: Prefix
            backend:
              service:
                name: airbyte-airbyte-connector-builder-server-svc
                port:
                  number: 80
          # API -> server (8001)
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: airbyte-airbyte-server-svc
                port:
                  number: 8001
          - path: /v1
            pathType: Prefix
            backend:
              service:
                name: airbyte-airbyte-server-svc
                port:
                  number: 8001
          # Web UI -> webapp (8000); must be last so / catches everything else
          - path: /
            pathType: Prefix
            backend:
              service:
                name: airbyte-airbyte-webapp-svc
                port:
                  number: 8000
  tls:
    - hosts:
        - airbyte.${SECRET_DOMAIN}
