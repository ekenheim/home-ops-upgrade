---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./pipeline
  - ./service
  # - ./db   # Omit: development uses Crunchy PGO; ensure postgres-pguser-bandit secret exists
  # Seldon v2 Model+Experiment CRs live in datasci/seldon-core/app/bandit
  # (targetNamespace here forces development; Seldon runtime is in datasci)
