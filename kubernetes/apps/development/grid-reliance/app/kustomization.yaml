---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
# API runs in datasci (grid-reliance-api) with Rook Gold credentials; see kubernetes/apps/datasci/grid-reliance.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  # - ./api  # Moved to datasci; API there uses Gold OBC (grid-resilience-gold).
  - ./pipeline
  - ./dashboard
  # - ./generator  # Re-enable when ghcr.io/ekenheim/grid-resilience-generator:v1.0.0 is published
