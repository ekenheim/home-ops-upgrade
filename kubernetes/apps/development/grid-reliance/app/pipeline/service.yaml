---
# Service for Dagster gRPC code location (grid-reliance-pipeline.development.svc.cluster.local:4000).
apiVersion: v1
kind: Service
metadata:
  name: grid-reliance-pipeline
  namespace: development
  labels:
    app.kubernetes.io/name: grid-reliance
    app.kubernetes.io/component: pipeline
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 4000
      targetPort: grpc
      protocol: TCP
  selector:
    app.kubernetes.io/name: grid-reliance
    app.kubernetes.io/component: pipeline
