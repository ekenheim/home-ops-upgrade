---
# Nordic Housing scraper: runs on schedule, writes to MinIO (config/state/raw).
# Image: in-cluster Harbor registry (harbor-registry.development.svc.cluster.local:5000).
apiVersion: batch/v1
kind: CronJob
metadata:
  name: hempriser-scraper
  namespace: development
  labels:
    app.kubernetes.io/name: hempriser
    app.kubernetes.io/component: scraper
spec:
  schedule: "0 * * * *"  # hourly; adjust as needed
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          restartPolicy: OnFailure
          envFrom:
            - secretRef:
                name: hempriser-minio
          containers:
            - name: scraper
              image: harbor-registry.development.svc.cluster.local:5000/ekenheim/nordic-housing-scraper:latest
              imagePullPolicy: IfNotPresent
              # Scraper uses MINIO_ENDPOINT, MINIO_ACCESS_KEY, MINIO_SECRET_KEY from hempriser-minio
              resources:
                requests:
                  cpu: 50m
                  memory: 128Mi
                limits:
                  memory: 512Mi
