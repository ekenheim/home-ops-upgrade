---
apiVersion: v1
kind: Service
metadata:
  name: rybbit-clickhouse
  namespace: development
spec:
  selector:
    app.kubernetes.io/name: rybbit-clickhouse
  ports:
    - name: http
      port: 8123
      targetPort: http
    - name: native
      port: 9000
      targetPort: native
