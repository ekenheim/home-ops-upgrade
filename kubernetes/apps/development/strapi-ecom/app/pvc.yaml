---
# Standalone PVC for Strapi Ecom app data. VolSync disabled; content is backed up via Postgres.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "${CLAIM:-strapi-ecom-data}"
  labels:
    app.kubernetes.io/name: "${APP:-strapi-ecom}"
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: "${VOLSYNC_CAPACITY:-2Gi}"
  storageClassName: "ceph-block"
