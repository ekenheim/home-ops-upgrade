---
# Contains CiliumL2AnnouncementPolicy originally, but that conflicts with BGP.
# Leaving file to avoid breaking kustomization, but its content is effectively ignored
# as l2announcements.enabled=false should be set in Helm values when using BGP.
