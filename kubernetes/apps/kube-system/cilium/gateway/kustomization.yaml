---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system # Target namespace for these resources
resources:
  - crds.yaml # Ensure CRD Flux definitions are included
  - external.yaml
  - internal.yaml
  - certificate.yaml # Add the certificate definition
  # - certificate.yaml # Add this back if/when cert-manager is confirmed
  - redirect.yaml
