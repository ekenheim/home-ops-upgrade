---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  # Mounted into the agent Deployment by the HelmRelease's postRenderer.
  - ./gitconfig-configmap.yaml
  - ./ocirepository.yaml
  - ./helmrelease.yaml
